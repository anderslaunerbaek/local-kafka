```sql
CREATE STREAM FORBRUGSCHEMA (
  ID VARCHAR KEY,
  DT_START TIMESTAMP,
  DT_END TIMESTAMP,
  VALUE FLOAT
) WITH (
  KAFKA_TOPIC = 'FORBRUGSCHEMA',
  PARTITIONS = 10, 
  VALUE_FORMAT = 'avro'
);    
```

```sql
CREATE STREAM PRISSCHEMA (
  ID VARCHAR KEY,
  DT_START TIMESTAMP,
  DT_END TIMESTAMP,
  VALUE FLOAT
) WITH (
  KAFKA_TOPIC = 'PRISSCHEMA',
  PARTITIONS = 10, 
  VALUE_FORMAT = 'avro'
);    
```